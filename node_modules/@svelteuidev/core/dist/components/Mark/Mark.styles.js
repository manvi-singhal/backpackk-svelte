import { createStyles } from '../../styles';
export default createStyles((theme, { color }) => {
    return {
        root: {
            backgroundColor: theme.fn.themeColor(color, 2),
            darkMode: {
                backgroundColor: theme.fn.themeColor(color, 5),
                color: theme.colors.dark900.value
            }
        }
    };
});
